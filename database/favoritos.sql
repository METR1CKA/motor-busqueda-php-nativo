CREATE TABLE
    favoritos (
        id INT AUTO_INCREMENT PRIMARY KEY,
        title_m VARCHAR(255) NOT NULL,
        year_m VARCHAR(255) NOT NULL,
        type_m VARCHAR(255) NOT NULL,
        poster_m TEXT NOT NULL
    );